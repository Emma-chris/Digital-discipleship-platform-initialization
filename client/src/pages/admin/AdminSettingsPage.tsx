import { AdminPanel } from './AdminPanel';

export default function AdminSettingsPage() {
  return (
    <AdminPanel
      title="Settings"
      description="Platform-level configuration, organization details and global preferences."
    />
  );
}