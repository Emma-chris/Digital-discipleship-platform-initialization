import { AdminPanel } from './AdminPanel';

export default function AdminCoursesPage() {
  return (
    <AdminPanel
      title="Courses"
      description="Create and publish courses, modules, lessons and assessments."
    />
  );
}